make:

Используется для создания слайсов, карт и каналов.
Пример использования:

    m := make(map[string]int)
    s := make([]int, 0)
    ch := make(chan int)
make инициализирует и возвращает значение (не нулевое) для указанных типов данных
(которые требуют инициализации).

new:

Используется для создания указателей на новую память выделенную под значение типа 
переданного в качестве аргумента.

Пример использования:

    p := new(int)
new возвращает указатель на нулевое значение для указанного типа данных.
Итак, make используется для создания слайсов, карт и каналов,
тогда как new используется для выделения памяти и возвращает указатель 
на нулевое значение указанного типа данных.