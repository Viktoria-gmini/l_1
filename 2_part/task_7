В какой последовательности будут выведены элементы map[int]int?

Пример:
m[0]=1
m[1]=124
m[2]=281

Ответ:

Нет определённой последовательности, эта структура данных не предусматривает
какое-либо позиционирование элементов, а только хранит соответствия ключей и их значений